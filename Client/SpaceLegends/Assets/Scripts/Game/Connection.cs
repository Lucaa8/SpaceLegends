using UnityEngine;

public class Connection : MonoBehaviour
{


    void Awake()
    {
        if (Auth.Instance != null)
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
